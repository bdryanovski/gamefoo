{
  "name": "experiment00",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Experiment00 — a minimal gamefoo engine boot (reads the engine from source).",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "mixpanel-browser": "^2.65.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
