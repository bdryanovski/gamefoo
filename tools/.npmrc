# Don't block `pnpm dev`/`pnpm start` on the pre-run dependency check.
# Without this, a lockfile/node_modules drift makes pnpm prompt to reinstall
# node_modules, which can stop the API server from launching and cause
# "Failed to save project to server".
verify-deps-before-run=false
