/**
 * Representation of input keys provided from external controllers
 *
 * @since 0.4.0
 */
export enum INPUT_KEY {
  SPACE = ' ',
}

/**
 * Input controls is developer friendly name for predefined controlled scheme
 *
 * @since 0.4.0
 */
export enum INPUT_CONTROLS {
  UP = 'w',
  DOWN = 's',
  LEFT = 'a',
  RIGHT = 'd',
  PRIMARY = 'o',
  SECONDARY = 'p',
  SELECT = 'b',
  START = 'n',
}
